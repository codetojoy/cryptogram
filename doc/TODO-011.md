
### TODO-011 — COMPLETE

* Refactor UI so that the current styling is a theme "Original". ✅
    * The colour tokens in `src/routes/+layout.svelte` are now explicitly the "Original" theme (base `:root`); fonts stay shared. Additional themes override only the colour tokens via a `:root[data-theme='<id>']` block. Components already consume the tokens, so switching `data-theme` re-skins the whole app with no component changes — the one hardcoded colour (the selected-cell wash in `play/+page.svelte`) was tokenised as `--accent-tint` so it re-skins too.
* Introduce a new theme called "Prince". ✅
    * Dark royal purple per the artist's aesthetic: deep aubergine paper (`--bg #241436`), lavender-white ink (`#f3ecff`), vivid violet accents (`--accent #9d4edd`, `--accent-deep #7b2cbf`). ✅
    * Secondary colour: **gold** (`--focus #e8b84b`) for focus rings / highlights — reads strongly against the dark purple. Solved/error states use `#4ade80` / `#f87171` so they stay legible on the dark background. ✅
* Update config setting to offer "Original" and "Prince" to the user. ✅
    * New central theme registry `src/lib/ui/theme.ts` (`THEMES`, `themeFor`, `themeId`, `applyTheme`); the config `<select>` is driven by it, so adding a theme is a one-line data change.
    * Store selection in local storage. ✅ — persisted via the existing `saveSettings` (key `cryptogram.settings.v1`, stores the label). Applied app-wide on Save and re-asserted on every layout mount; a tiny inline script in `src/app.html` sets `data-theme` (and the browser `theme-color`) **before first paint**, so Prince users get no flash of the Original palette on cold load. No network (privacy, SPEC §3).

* bump version to 0.2.11 (minor version increase) ✅

**Notes:** No new third-party asset (colours are project-original), so `ASSETS.md` is unchanged. Verified with `npm run check` (0 errors, 346 files), 45 unit tests (added 4 theme tests: registry offers both, label→slug mapping, unknown→Original fallback, `applyTheme` SSR-safe no-op), a clean production build, and an end-to-end browser drive: the dropdown offers Original/Prince; choosing Prince + Save flips `data-theme` to `prince`, turns the body background aubergine (`#241436`), sets `theme-color` to `#7b2cbf`, and persists `{"theme":"Prince"}`; a fresh load of `/play` renders Prince immediately (anti-flash confirmed); switching back to Original reverts and persists.
